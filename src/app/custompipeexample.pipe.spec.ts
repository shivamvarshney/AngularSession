import { CustompipeexamplePipe } from './custompipeexample.pipe';

describe('CustompipeexamplePipe', () => {
  it('create an instance', () => {
    const pipe = new CustompipeexamplePipe();
    expect(pipe).toBeTruthy();
  });
});
